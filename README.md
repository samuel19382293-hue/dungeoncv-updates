# Dungeon Scripts CV — updates

This repo hosts only compiled release downloads (`.exe`) and a `version.json`
pointer — no source code. Dungeon Scripts.exe checks this repo on launch to
offer in-place updates.
